## This repo contains YAML files for deploying <https://github.com/dockersamples/example-voting-app> applicaiton

- Run this command to apply all the files in your cluster

    kubectl apply -f ./kubernetes -R
